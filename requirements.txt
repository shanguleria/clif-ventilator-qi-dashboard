# One shared environment for the whole bundle: the LPV pipeline + the scorecard combiner +
# every folded-in metric (proning, sat, ...). Union of each metric's needs (highest pin wins).
clifpy>=0.4.9
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
duckdb>=0.10
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
plotly>=5.0
tabulate>=0.9
tableone>=0.9
jinja2>=3.1
Pillow>=10.0
jupyter>=1.0
ipykernel>=6.29
