# CLIF Ventilator QI Bundle — Methods Index

A **living methods / data-dictionary reference** for every component of the QI bundle scorecard. When
someone asks "how did you define the denominator for a given month's SAT rate?" the answer lives here —
and it can't silently go stale: the specific numbers, thresholds, category lists, and versions in each
doc are machine-stamped from the pipeline (`config.json` + the tile feed) by `docs/build_methods.py`,
which runs at the end of every scorecard rebuild.

**How to read these docs:** the hand-written prose describes the *logic* of each definition; the
`AUTOGEN` block at the bottom of each doc carries the *current* facts (exact thresholds, drug/mode lists,
headline num/den/rate, and the provenance block: `definition_version`, code SHA, `clif_version`,
generated date). If the two ever disagree, the AUTOGEN block is authoritative and the prose needs a pass.

## Components

- **Per-metric methods** — one doc per tile (linked in the table below).
- **Combiner** — [`scorecard_methods.md`](scorecard_methods.md): how the tiles are assembled, grain
  fallback, unit grouping.
- **Tile-feed contract** — [`../contract/tile_feed_contract.md`](../contract/tile_feed_contract.md):
  the PHI-free feed schema every metric emits, and the provenance/pooling rules.

## Tiles at a glance

<!-- AUTOGEN:START -->

*The block below is machine-generated by `docs/build_methods.py` from the shared `definitions/` + pipeline code — do not edit by hand; edits are overwritten on the next refresh. It holds only site-invariant **definitions**; current per-site numbers live in each tile feed + `output/<site>/output_to_share/manifest.json`.*

| Tile | Definition version | Methods doc |
|---|---|---|
| **LPV Adherence** | `lpv-v1` | [`metrics/lpv/METHODS.md`](../metrics/lpv/METHODS.md) |
| **ARDS Proning** | `proning-v1` | [`metrics/proning/METHODS.md`](../metrics/proning/METHODS.md) |
| **Spontaneous Awakening Trial** | `sat-v1` | [`metrics/sat/METHODS.md`](../metrics/sat/METHODS.md) |
| **Spontaneous Breathing Trial** | `sbt-v1` | [`metrics/sbt/METHODS.md`](../metrics/sbt/METHODS.md) |


_Current headline numbers per site live in each tile feed + `output/<site>/output_to_share/manifest.json`._

<!-- AUTOGEN:END -->
